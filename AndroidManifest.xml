<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rudik_maksim.cde"
    android:versionCode="17"
    android:versionName="@string/app_version" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_release"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

<!-- ACTIVITIES -->

        <activity
            android:name=".ActivityLogin"
            android:label="@string/authorization"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" >
        </activity>
        <activity
            android:name=".ActivityPoints"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitySubjectDetails"/>
        <activity android:name=".ActivityScheduleTeacher" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>>
        <!--activity android:name=".widgets.ScheduleWidgetConfigActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity-->

<!-- RECEIVERS -->

        <!--receiver android:name=".widgets.ScheduleWidget"
            android:icon="@drawable/de_icon_big"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_info" />
        </receiver-->

<!-- SERVICES -->

        <service
            android:name=".services.NewPointsNotificationService"
            android:enabled="true" />

        <!--service
            android:name=".services.ScheduleWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" /-->
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <uses-permission android:name="android.content.Intent.ACTION_SENDTO" />

</manifest>
